nav.nav
	.nav-left
		a.nav-item(href='/')
			| Photo Bazaar
	span.nav-toggle
		span
		span
		span
	.nav-right.nav-menu
		if myName
			a#bazaar.nav-item(href="/bazaar")
				p Bazaars
			a#login.nav-item(href="/user")
				p= myName
		else
			a#login.nav-item(href='#') Login